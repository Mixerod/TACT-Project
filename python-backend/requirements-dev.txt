# Test/dev-only dependencies (kept out of requirements.txt so the PyInstaller
# build stays lean). Install with:  pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.0.3
# httpx is required by FastAPI's TestClient. Pin <0.28 because starlette 0.36.3
# (bundled with fastapi 0.110.0) uses the 'app=' TestClient shortcut that was
# removed in httpx 0.28.
httpx==0.27.2
