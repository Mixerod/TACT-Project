[pytest]
# Collect only the maintained suite under tests/. This deliberately excludes the
# legacy root-level test_*.py scripts (which target a live server / old paths).
testpaths = tests
addopts = -v --tb=short
